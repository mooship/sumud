pre-commit:
  commands:
    eslint:
      glob: "*.{ts,tsx}"
      run: pnpm exec eslint --fix {staged_files}
      stage_fixed: true
    prettier:
      run: pnpm exec prettier --write --ignore-unknown {staged_files}
      stage_fixed: true
    vitest:
      run: pnpm run test
