// Shared with the English further-reading page -- the component itself reads the locale from
// the URL (see src/routes/further-reading/index.tsx).
export { default, head } from "~/routes/further-reading/index";
