// Shared with the English take-action page -- the component itself reads the locale from the
// URL (see src/routes/take-action/index.tsx).
export { default, head } from "~/routes/take-action/index";
