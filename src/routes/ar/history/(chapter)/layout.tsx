// Shared with the English history chapters -- the layout itself reads the locale from the URL
// (see src/routes/history/(chapter)/layout.tsx), so it needs no Arabic-specific copy here.
export { default } from "~/routes/history/(chapter)/layout";
